#include <iostream>

using namespace std;

namespace ariel {

    //read_units(units_file);

    class NumberWithUnits {
        private:
        
        public: 
            void read_units(ifstream stream);
    };
};